# frontend/src/ee — Enterprise UI (proprietary)

Placeholder. Enterprise-only React code will live here and is compiled **only**
into the `tesqivo-enterprise` image (a separate Vite build that includes this
directory). Nothing in the Community build imports from `ee/`, so `npm run build`
and `tsc` ignore it.

Licence: proprietary — see `./LICENSE` and `/LICENSING.md`.
