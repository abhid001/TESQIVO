# Used only by docker-compose.tls.yml. Caddy obtains and renews a Let's Encrypt
# certificate for {$TESQIVO_DOMAIN} automatically and proxies to the app.

{$TESQIVO_DOMAIN} {
	encode gzip
	reverse_proxy web:8080
}
